<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Books Repository</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
  <div class="container">
    <%- include('partials/header.ejs') %>

    <div class="container">
      <div class="px-4 py-5 my-5 text-center">
        <img class="d-block mx-auto mb-4" src="<%= book.cover %>" alt="" width="172" height="257">
        <h1 class="display-5 fw-bold"><%= book.title %></h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4 fw-bold">Authors:
           <% if (book.authors && book.authors.length > 0) { %>
            <%= book.authors.map(author => author.name).join(', ') %>
          <% } else { %>
              No hay autores registrados
          <% } %>
          </p>
          <p class="lead mb-4 fw-bold">Release Date: </p>
          <p class="lead mb-4"><%= book.description %></p>
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/books/edit/<%=book.id%>"> <button type="button" class="btn btn-primary btn-lg px-4 gap-3">Edit</button></a>
            <form action="/books/edit/<%=book.id%>?_method=DELETE" method="POST">
              <button type="submit" class="btn btn-outline-secondary btn-lg px-4">Delete</button>
            </form>
          </div>
        </div>
      </div>
  </div>
</div>


<%- include('partials/footer.ejs') %>
</body>
</html>