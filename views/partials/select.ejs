        <div class="row align-items-center py-3">
          <div class="col-md-3 ps-5">
            <h6 class="mb-0">Book</h6>
          </div>
          <div class="col-md-6 pe-5">
            <select class="form-select" name="bookAuthor">
              <% if(locals.book) { %>
                <% {book.forEach(element => {%>
                  <option value="<%= element.title %>" 
                    <%= (locals.oldData && locals.oldData.book == element.title) ? 'selected' : '' %> >
                    <%= element.title %>
                  </option>
                <% })} %>
                  <option id="addAuthor" type ="button"> add</option>
              <% } %>
            </select> 
          </div>
          <button type="button" class=" col-md-1 btn btn-primary">+</button>
          <button id='quit' type="button" class=" col-md-1 btn btn-primary">-</button>
        </div>
