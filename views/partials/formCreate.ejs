
              <div class="row align-items-center pt-4 pb-3">
                <div class="col-md-3 ps-5">
                  <h6 class="mb-0">Complete name</h6>
                </div>
                <div class="col-md-9 pe-5">
                  <input type="text" name="author" 
                  class="form-control form-control-lg"  requerid 
                  value="<%= (locals.author?.name ?? locals.oldData?.author) || '' %>" >
                 <% if (locals.errors && errors.author) { %>
                      <p class="text-danger"><%=errors.author.msg %></p>
                  <%}%>
                </div>
              </div>      